import styled from "styled-components";
import { gold } from "../colors";

export const Header = styled.div`
  margin: auto;
  color: ${gold};
  font-size: 38px;
  letter-spacing: 2px;
  font-weight: bold;
  margin-bottom: 15px;
`;
